using UnityEngine;

// No longer used — dash is driven directly by SpellCaster.UpdateDash().
// Kept as an empty stub to avoid missing-script errors on any existing GameObjects.
public class SpellDash : MonoBehaviour
{
    public bool IsActive => false;
}
